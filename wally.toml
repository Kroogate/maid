[package]
name = "kroogate/maid"
version = "0.4.0"
registry = "https://github.com/UpliftGames/wally-index"
realm = "shared"
exclude = ["**"]
include = ["src", "src/init.lua", "default.project.json", "wally.lock", "wally.toml"]

[dependencies]
